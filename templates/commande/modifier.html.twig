{% extends 'base.html.twig' %}

{% block title %}Modifier Client

{% endblock %}
{% form_theme commande_form 'bootstrap_4_layout.html.twig' %}

{% block body %}
	{% if is_granted('ROLE_ADMIN') %}
		{{ form_start(commande_form) }}


		{{ form_widget(commande_form)}}
		{{ form_end(commande_form) }}
	{% endif %}
{% endblock %}
